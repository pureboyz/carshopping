<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cars.dao.MemberDao">
	<select id="login" resultType="com.cars.vo.MemberVo">
		select * from member
		where mId=#{mId} and mPass=#{mPass}
	</select>
	
	<select id="getUser" resultType="com.cars.vo.MemberVo">
		select * from member
		where mid=#{mid}
	</select>
</mapper>